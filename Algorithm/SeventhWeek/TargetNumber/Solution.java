package SeventhWeek.TargetNumber;
import java.util.*;

class Solution {
    public int solution(int[] numbers, int target) {
        int answer = 0;
        int len = numbers.length;
        boolean[] visited = new boolean[numbers.length];

        return answer;
    }
    public void dfs(int[] numbers, int target, int len, boolean[] visited, int count){
        if(target == 0&& len ==0){
            return;
        }
        for(int i = 0; i < numbers.length; i++){
            if
        }
    }
}
